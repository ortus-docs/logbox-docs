# TracerAppender

This appender directs messages via the `cftrace` tag. It has no configuration properties.

{% hint style="info" %}
The underlying class is `TracerAppender.cfc` (in `coldbox.system.logging.appenders`).
{% endhint %}
